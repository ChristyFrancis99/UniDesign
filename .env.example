# Optional: host the GLB on a CDN/object store for production deployments.
# Leave empty to use the bundled /models/interior-room.glb.
# Example: VITE_INTERIOR_MODEL_URL=https://cdn.example.com/interior-room.glb
VITE_INTERIOR_MODEL_URL=
